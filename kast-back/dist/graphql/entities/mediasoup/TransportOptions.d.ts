export default class TransportOptions {
    constructor(id: string, iceCandidates: object, iceParameters: object, dtlsParameters: object);
    id: string;
    iceCandidates: object;
    iceParameters: object;
    dtlsParameters: object;
}
